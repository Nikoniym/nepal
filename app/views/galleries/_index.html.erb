<% @images.each do |image| %>
    <div class="img-thumbnail">
      <%= image_tag image.image.url(:thumb), alt: image.image.url(:thumb) %>
      <%= link_to 'Destroy', gallery_path(image),
                  method: :delete %>
    </div>
<% end %>